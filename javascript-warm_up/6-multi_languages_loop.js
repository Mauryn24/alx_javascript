#!/usr/bin/bash/node

//create a string of arrays
const  strings = ["C is fun", "Python is cool", "JavaScript is amazing"];
//initialize a variable i
let i = 0;
//check if i is less than the length of the array
while (i < strings.length) {
    //print the string
    console.log(strings[i]);
    //increment
    i++;
}